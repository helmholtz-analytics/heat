import torch
import heat as ht

import h5py
import numpy as np
import base64
import os

__all__ = [""]


def load_image_from_buffer(img, to_gpu=False):
    """
    thing function will load the image from a string and rebuild the image on the CPU
    then send it to the GPU (if specified).

    Parameters
    ----------
    img

    Returns
    -------

    """

    pass
