name: benchmarking-test

on:
  push: 
    branches: [ bencharking-with-pytest-for-genkgo ]
    
    
jobs:
  run-benchmarks:
    name: benchmark automation
    runs-on: self-hosted
    steps:
      - uses: fifsky/ssh-action@master
        with:
          command: |
            ls -a
          host: ${{ secrets.HDFML_HOST }}
          user: berhanu1
          key: ${{ secrets.HDFML_KEY}}

